{
  "name": "wiperx-web",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the WiperX web UI (Tailwind CSS). Dev-only; the built stylesheet at web/static/css/app.css is committed.",
  "scripts": {
    "build:css": "tailwindcss -i web/static/src/app.css -o web/static/css/app.css --minify",
    "watch:css": "tailwindcss -i web/static/src/app.css -o web/static/css/app.css --watch",
    "check:css": "npm run build:css && git diff --exit-code -- web/static/css/app.css"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.9",
    "@tailwindcss/typography": "^0.5.15",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14"
  }
}

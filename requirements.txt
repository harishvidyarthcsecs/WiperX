# WiperX - Production-Grade Cross-Platform Disk Wiping System
# Core Dependencies

# Web Framework
Flask==3.0.2
Flask-Login==0.6.3
Flask-WTF==1.2.1
Werkzeug==3.0.1

# Remote Execution
paramiko==3.4.0          # SSH (Linux remote)
pywinrm==0.4.3           # WinRM (Windows remote)
requests-credssp==2.0.0  # WinRM CredSSP auth

# Reporting
reportlab==4.1.0         # PDF certificate generation
Jinja2==3.1.3            # Template rendering

# Utilities
psutil==5.9.8            # Local disk/process info
python-dotenv==1.0.1     # Environment variable management
colorama==0.4.6          # CLI color output
tabulate==0.9.0          # CLI table formatting
click==8.1.7             # CLI argument parsing

# Security
cryptography==42.0.4     # Key management helpers
bcrypt==4.1.2            # Password hashing

# Logging & Monitoring
python-json-logger==2.0.7  # Structured JSON logging

# Module 3 — Forensic Carving & Recovery
pytsk3==20240220           # The Sleuth Kit bindings: filesystem walk + undelete
python-magic==0.4.27       # libmagic content classification (needs libmagic system pkg)
Pillow==10.3.0             # image validator (JPEG/PNG structural verify)
pypdf==4.2.0               # PDF validator / structural parse
mutagen==1.47.0            # audio/video container validator
# libewf-python            # OPTIONAL: E01 evidence image support (install from distro pkg)

# System packages (install separately, NOT via pip):
#   Linux:  apt install sleuthkit testdisk libmagic1 nvme-cli hdparm util-linux
#   macOS:  brew install sleuthkit libmagic   (dev host only; runtime target is Linux)

# Testing
pytest==8.1.1
pytest-mock==3.12.0

# Development
black==24.3.0
flake8==7.0.0
